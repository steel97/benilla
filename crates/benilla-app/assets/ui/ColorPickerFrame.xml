<Ui>
    <!--
        The shared colour dialog — our own implementation of the window that
        Interface\FrameXML\ColorPickerFrame.xml specifies. Line numbers below are that file's (336
        lines, XML only; it ships no `.lua`, its whole script is inline and the one function that
        drives it, `UIDropDownMenuButton_OpenColorPicker`, lives in UIDropDownMenu.lua). It is
        cited throughout as the SPECIFICATION — for the names, the geometry, the sounds and the
        call order, all of which are the API addons are written against and none of which we are
        free to invent. The Lua in this file is ours.

        WHY IT IS HERE, in the arc's own terms: **86 of the 218 corpus addons reach
        `ColorPickerFrame`, 80 reach `OpacitySliderFrame`** — and 1207's lesson applies before those
        numbers do, so read the lines. 64 of the files are copies of `Dewdrop-2.0.lua`, which does
        not roll its own picker: it drives THIS one, with the exact block below (Dewdrop-2.0.lua
        l.425-465, verbatim from a corpus copy):

            ColorPickerFrame.func        = function() … ColorPickerFrame:GetColorRGB() …
                                                       OpacitySliderFrame:GetValue() … end
            ColorPickerFrame.hasOpacity  = this.hasOpacity
            ColorPickerFrame.opacityFunc = ColorPickerFrame.func
            ColorPickerFrame.opacity     = 1 - this.opacity
            ColorPickerFrame:SetColorRGB(this.r, this.g, this.b)
            ColorPickerFrame.cancelFunc  = function() … end
            ShowUIPanel(ColorPickerFrame)

        Dewdrop is *the* Ace2 menu library, so that block is how EVERY colour option in EVERY Ace2
        addon's config menu opens. On top of it sit ~14 addons that name the frame directly
        (TipBuddy, CT_UnitFrames, CT_RaidAssist, CooldownCount, PowerAuras, Bongos_Options,
        MikScrollingBattleText, EQL3, RangeColor, ColorPickerPlus, QuestHistory, Radar,
        IgniteStatus, Bagnon_Core), and `AceConsole-2.0.lua` l.1402-1406, which does
        `ColorPickerOkayButton:GetScript("OnClick")` and chains it — so the two BUTTON names are API,
        not decoration.

        The widget itself (`<ColorSelect>`, `SetColorRGB`/`GetColorRGB`/`OnColorSelect`) is
        engine-side, in `benilla-ui/src/script/colorselect.rs` — read its module doc before touching
        anything colour-valued here. Two things live there that are not obvious and are byte-verified
        (wow-re `system/ui/scratch/colorselect-color-law.md`, a §5 cross-check dispatched from this
        repo for exactly this window): the widget's state is **HSV floats**, not RGB, and the
        set→read round trip is deliberately **not** the identity — 9.75 % of colours come back one
        step low on a channel, and the FrameXML `GetColorRGB … SetColorRGB` idiom ratchets. It also
        says why the widget could not be a few methods hung on this one frame (TipBuddy declares two
        `<ColorSelect>` frames of its own).

        ── THE WHEEL IS ENGINE ART, AND THAT IS WHY IT TOOK A §5 ───────────────────────────────────
        Look at what `<ColorWheelTexture>` and `<ColorValueTexture>` below do NOT have: a `file=`.
        The hue disc and the brightness strip are **generated by the client**, not loaded — there is
        no BLP anywhere in the MPQ chain that is a colour wheel, only the two little thumb markers
        that ride on top of them. So the four elements are declared here for their names and their
        geometry, and the *pixels* are the app renderer's
        (`benilla-app/src/ui_script/extract/colorselect.rs`), the same division of labour the
        `<Minimap>` widget's content hole uses.

        For two years this file shipped without them and said so. What unblocked it was wow-re's
        `system/ui/scratch/colorselect-drawn-appearance.md` (decision 1592), which carved what the
        client actually draws — a 128×128 generated ARGB texture on an integer lattice with a hard
        alpha cut, and a solid `HSV(H,S,1)` tile under a black→white vertex gradient — and, in
        passing, corrected the obvious guess: **the wheel is drawn at a literal `V = 1.0` and never
        reads the widget's brightness.** Lowering the opacity/value marker moves the marker; the
        disc behind it does not dim. Read that module's header before touching any of this.

        The two thumbs carry `file=` and **no `<Anchors>`**, which is not an omission either: the
        client `ClearAllPoints`es them and re-`SetPoint`s from C++ on every colour change
        (`0x78b850`/`0x78ba90`), so an authored anchor would be discarded. benilla derives their
        rects the same way, in `script::colorselect::wheel_thumb_rect`/`value_thumb_rect` — including
        the client's two asymmetries (the wheel marker scales its *vertical* offset by the wheel's
        **width**; the value marker scales by the **wheel's** height while anchoring to the strip).

        OTHER DEVIATIONS, each forced:
        · **`<OnKeyDown>` (ref l.177-184) is not transcribed.** The engine reason is GONE — decision
          1319 built the frame key-down walk and `OnKeyDown` is a live script kind (StackSplit.xml
          declares one), so this could now be transcribed as written. It is not, because transcribing
          it changes more than ESC: the walk's gate is EXISTENCE, so the frame would begin swallowing
          EVERY key while the picker is open, and the UiPanels ESC rung below would have to come out
          in the same move. That is a deliberate deferral now, not a wall. Its ESCAPE arm is
          `HideUIPanel(this) + cancelFunc(previousValues)`, i.e.
          exactly what the Cancel button does, so the arm is carried in UiPanels.xml's ESC chain as
          `ColorPickerCancelButton:Click()` — the reference's own idiom for this (its game-menu
          chain cancel-clicks `OptionsFrameCancel` the same way). ESC therefore CANCELS, it does not
          merely hide, which is the half people get wrong.
        · **`OpacityFrame` (ref l.223-335) IS transcribed now** — the second frame in the reference's
          file: a standalone 80x180 opacity popup with its own vertical slider and a fullscreen
          click-off-to-close button, used by FloatingChatFrame's chat-window alpha. This note used
          to read "**zero corpus callers** and no benilla consumer, so it waits for its first
          customer (decision 1195)". Its first customer arrived: pfUI skins it in two files —
          `skins/blizzard/miscellaneous.lua` l.36-41 (`CreateBackdrop(OpacityFrame)`,
          `SkinSlider(OpacityFrameSlider)`, then a re-anchor) and
          `skins/blizzard/battlefield_minimap.lua` l.23-24 (re-anchors `OpacityFrame` off the
          battlefield minimap). 1195's own bar is demand, so demand is what moves it (1719).
          It is transcribed verbatim, `opacityFunc`/`saveOpacityFunc` hooks included — those are
          the whole contract with a caller, and a popup that drops them would move a slider that
          changes nothing.
        · **The unguarded `ColorPickerFrame.func()` in Okay's OnClick (ref l.89) is KEPT unguarded.**
          It is one of the few places quirk-for-quirk beats tidying: an addon that opened this window
          without setting `func` errors in the real client too, and a guard here would swallow that
          into a window that silently does nothing.
        · `<ColorSelect>` carries no `<Layers>` `hidden` art beyond the reference's three regions;
          `ColorSwatch` and `ColorPickerFrameHeader` keep their unprefixed reference names, which is
          required — `ColorPickerFrame.xml`'s own `<OnColorSelect>` addresses `ColorSwatch` by name,
          and so would any addon that re-skins the window.
    -->

    <Script><![CDATA[
        -- ref GlobalStrings.lua l.770 / l.132 / l.1458 (enUS), in the `X = X or "…"` FALLBACK form
        -- this file set uses throughout: the app runs the real localized GlobalStrings.lua off the
        -- patch chain BEFORE the shipped XML, so these only bind on a surface without it (a headless
        -- test). `text="COLOR_PICKER"` / `text="OKAY"` / `text="CANCEL"` below are global-string
        -- KEYS, not literals — the loader resolves each through the Lua global (rf28 `0x703bf0`).
        COLOR_PICKER = COLOR_PICKER or "Color Picker"
        OKAY = OKAY or "Okay"
        CANCEL = CANCEL or "Cancel"
    ]]></Script>

    <!-- ref l.3-9. 365 wide is the WITH-opacity width; OnShow narrows it to 305 when an addon did
         not ask for the slider. `movable="true"` is the reference's (this engine only honours it
         once something calls StartMoving, which nothing here does — same as the reference, whose
         frame has no drag handler either: the attribute is there for addons). -->
    <ColorSelect name="ColorPickerFrame" toplevel="true" parent="UIParent" movable="true" enableMouse="true" enableKeyboard="true" hidden="true">
        <Size><AbsDimension x="365" y="200"/></Size>
        <Anchors><Anchor point="CENTER"/></Anchors>
        <Backdrop bgFile="Interface\DialogFrame\UI-DialogBox-Background" edgeFile="Interface\DialogFrame\UI-DialogBox-Border" tile="true">
            <BackgroundInsets><AbsInset left="11" right="12" top="12" bottom="11"/></BackgroundInsets>
            <TileSize><AbsValue val="32"/></TileSize>
            <EdgeSize><AbsValue val="32"/></EdgeSize>
        </Backdrop>
        <Layers>
            <Layer level="ARTWORK">
                <!-- ref l.23-35. The live preview: a 32x32 solid quad repainted from OnColorSelect.
                     Unprefixed name, the reference's own — see the header. -->
                <Texture name="ColorSwatch">
                    <Size><AbsDimension x="32" y="32"/></Size>
                    <Anchors><Anchor point="TOPLEFT"><Offset><AbsDimension x="225" y="-32"/></Offset></Anchor></Anchors>
                    <Color r="1.0" g="1.0" b="1.0" a="1.0"/>
                </Texture>
                <!-- ref l.36-47. The same header plate the game menu and every dialog wears. -->
                <Texture name="ColorPickerFrameHeader" file="Interface\DialogFrame\UI-DialogBox-Header">
                    <Size><AbsDimension x="256" y="64"/></Size>
                    <Anchors><Anchor point="TOP"><Offset><AbsDimension x="0" y="12"/></Offset></Anchor></Anchors>
                </Texture>
                <!-- ref l.48-56. Unnamed in the reference too. -->
                <FontString inherits="GameFontNormal" text="COLOR_PICKER">
                    <Anchors>
                        <Anchor point="TOP" relativeTo="ColorPickerFrameHeader"><Offset><AbsDimension x="0" y="-14"/></Offset></Anchor>
                    </Anchors>
                </FontString>
            </Layer>
        </Layers>
        <Frames>
            <!-- ref l.60-77 for the name, the geometry and the sound. Cancel is declared FIRST
                 because Okay anchors to it. -->
            <Button name="ColorPickerCancelButton" inherits="GameMenuButtonTemplate" text="CANCEL">
                <Anchors>
                    <Anchor point="BOTTOMRIGHT"><Offset><AbsDimension x="-10" y="10"/></Offset></Anchor>
                </Anchors>
                <Scripts>
                    <OnClick>
                        -- Dismiss first, undo second — the undo is the CALLER's code, and a
                        -- cancelFunc that opens a window of its own should not have to fight this
                        -- one for the panel slot.
                        HideUIPanel(this:GetParent());
                        -- This window remembers nothing itself: `previousValues` is the snapshot
                        -- whoever opened it left behind (`UIDropDownMenuButton_OpenColorPicker`
                        -- fills it in; Dewdrop closes over its own copy and ignores it) and
                        -- `cancelFunc` is the caller's restore. Both are optional fields on the
                        -- frame, so a caller that set neither simply gets the window closed.
                        local cancelFunc = ColorPickerFrame.cancelFunc;
                        if ( cancelFunc ) then
                            cancelFunc(ColorPickerFrame.previousValues);
                        end
                        PlaySound("igMainMenuOptionCheckBoxOn");
                    </OnClick>
                </Scripts>
            </Button>
            <!-- ref l.78-96. `func()` unguarded, `opacityFunc()` guarded — the reference's own
                 asymmetry, kept (header). AceConsole-2.0 fetches THIS handler with
                 `ColorPickerOkayButton:GetScript("OnClick")` and calls it from its own replacement,
                 so the name and the script slot are both load-bearing. -->
            <Button name="ColorPickerOkayButton" inherits="GameMenuButtonTemplate" text="OKAY">
                <Anchors>
                    <Anchor point="RIGHT" relativeTo="ColorPickerCancelButton" relativePoint="LEFT"><Offset><AbsDimension x="0" y="0"/></Offset></Anchor>
                </Anchors>
                <Scripts>
                    <OnClick>
                        HideUIPanel(this:GetParent());
                        -- Commit. Neither callback is handed the values: both read the widget and
                        -- the slider for themselves, which is the contract every corpus caller is
                        -- written against. `func` is deliberately NOT guarded (header) — a caller
                        -- that opened the window without one should hear about it. The alpha half
                        -- genuinely is optional: a caller that never asked for opacity has no
                        -- reason to supply one.
                        ColorPickerFrame.func();
                        local opacityFunc = ColorPickerFrame.opacityFunc;
                        if ( opacityFunc ) then
                            opacityFunc();
                        end
                        PlaySound("igMainMenuOptionCheckBoxOn");
                    </OnClick>
                </Scripts>
            </Button>
            <!-- ref l.97-158. A real Slider, so it drags: 0..1 in 0.01 steps, VERTICAL, seated to
                 the RIGHT of the swatch. Its `$parentText` label is EMPTY in the reference (only
                 OpacityFrame's copy carries text="OPACITY") — transcribed as written, not
                 "improved". Note the sense: 1 is FULLY OPAQUE, and every caller inverts
                 (`ColorPickerFrame.opacity = 1 - this.opacity`, and Dewdrop's func reads back
                 `1 - OpacitySliderFrame:GetValue()`). -->
            <Slider name="OpacitySliderFrame" orientation="VERTICAL" minValue="0" maxValue="1" valueStep="0.01" defaultValue="1">
                <Size><AbsDimension x="16" y="128"/></Size>
                <Anchors>
                    <Anchor point="TOPLEFT" relativeTo="ColorSwatch" relativePoint="TOPRIGHT"><Offset><AbsDimension x="32" y="0"/></Offset></Anchor>
                </Anchors>
                <Backdrop bgFile="Interface\Buttons\UI-SliderBar-Background" edgeFile="Interface\Buttons\UI-SliderBar-Border" tile="true">
                    <EdgeSize><AbsValue val="8"/></EdgeSize>
                    <TileSize><AbsValue val="8"/></TileSize>
                    <BackgroundInsets><AbsInset left="3" right="3" top="6" bottom="6"/></BackgroundInsets>
                </Backdrop>
                <Layers>
                    <Layer level="ARTWORK">
                        <FontString name="$parentText" inherits="GameFontNormalSmall">
                            <Anchors><Anchor point="BOTTOM" relativePoint="TOP"/></Anchors>
                        </FontString>
                        <FontString inherits="GameFontHighlightSmall" text="0%">
                            <Anchors>
                                <Anchor point="BOTTOMLEFT" relativePoint="BOTTOMRIGHT"><Offset><AbsDimension x="2" y="3"/></Offset></Anchor>
                            </Anchors>
                        </FontString>
                        <FontString inherits="GameFontHighlightSmall" text="100%">
                            <Anchors>
                                <Anchor point="TOPLEFT" relativePoint="TOPRIGHT"><Offset><AbsDimension x="-2" y="3"/></Offset></Anchor>
                            </Anchors>
                        </FontString>
                    </Layer>
                </Layers>
                <Scripts>
                    <OnValueChanged>
                        -- Live preview: every step of the drag reaches the caller, which is what
                        -- makes the alpha follow the thumb instead of jumping at Okay.
                        local opacityFunc = ColorPickerFrame.opacityFunc;
                        if ( opacityFunc ) then
                            opacityFunc();
                        end
                    </OnValueChanged>
                </Scripts>
                <ThumbTexture file="Interface\Buttons\UI-SliderBar-Button-Vertical">
                    <Size><AbsDimension x="32" y="32"/></Size>
                </ThumbTexture>
            </Slider>
        </Frames>
        <Scripts>
            <!-- ref l.161-170 for the two widths. The opacity slider is opt-in per caller, and the
                 window narrows by the slider's 60 units when nobody asked for it. -->
            <OnShow>
                -- Seeding the slider AFTER showing it is deliberate: SetValue fires
                -- OnValueChanged, so the caller's opacityFunc runs against a slider that is
                -- already on screen and against a window still at its old width — the reference's
                -- own order, and observable to any addon whose preview writes through. The width
                -- is settled once, at the end, instead of in both arms.
                local width = 305;
                if ( this.hasOpacity ) then
                    OpacitySliderFrame:Show();
                    OpacitySliderFrame:SetValue(this.opacity);
                    width = 365;
                else
                    OpacitySliderFrame:Hide();
                end
                this:SetWidth(width);
            </OnShow>
            <!-- ref l.171-176. The widget's own slot, fired by SetColorRGB with the colour the
                 widget now holds (colorselect.rs). This is the ONLY thing that paints the swatch —
                 no caller touches it — and it is also where an addon's live-preview `func` runs. -->
            <OnColorSelect>
                ColorSwatch:SetTexture(arg1, arg2, arg3);
                if ( this.func ) then
                    this.func();
                end
            </OnColorSelect>
        </Scripts>
        <!-- ref l.186-221. The reference declares all four AFTER `<Scripts>`, and the order is not
             cosmetic: `<ColorValueTexture>` anchors to `ColorPickerWheel` by name, so the wheel has
             to be published first. The wheel's name is the reference's own and unprefixed — an
             addon re-skinning the picker addresses it. -->
        <ColorWheelTexture name="ColorPickerWheel">
            <Size><AbsDimension x="128" y="128"/></Size>
            <Anchors>
                <Anchor point="TOPLEFT"><Offset><AbsDimension x="16" y="-32"/></Offset></Anchor>
            </Anchors>
        </ColorWheelTexture>
        <!-- The one BLP in this window: a 4x2 sheet of markers, of which these two crops are the
             wheel dot and the strip's arrow pair. Unnamed in the reference, and unanchored — see
             the header. -->
        <ColorWheelThumbTexture file="Interface\Buttons\UI-ColorPicker-Buttons">
            <Size><AbsDimension x="10" y="10"/></Size>
            <TexCoords left="0" right="0.15625" top="0" bottom="0.625"/>
        </ColorWheelThumbTexture>
        <ColorValueTexture>
            <Size><AbsDimension x="32" y="128"/></Size>
            <Anchors>
                <Anchor point="LEFT" relativeTo="ColorPickerWheel" relativePoint="RIGHT"><Offset><AbsDimension x="24" y="0"/></Offset></Anchor>
            </Anchors>
        </ColorValueTexture>
        <ColorValueThumbTexture file="Interface\Buttons\UI-ColorPicker-Buttons">
            <Size><AbsDimension x="48" y="14"/></Size>
            <TexCoords left="0.25" right="1.0" top="0" bottom="0.875"/>
        </ColorValueThumbTexture>
    </ColorSelect>

    <!-- ═══ ref l.223-335, verbatim ═══ The opacity popup: a standalone toplevel the colour
         picker's own file ships beside it, opened by whatever wants an alpha (FloatingChatFrame's
         chat-window alpha in the reference; pfUI re-anchors it off the battlefield minimap). It is
         NOT a child of ColorPickerFrame — `parent="UIParent"`, its own strata, its own show/hide.

         The two hook fields are the contract: `opacityFunc` fires on every slider tick,
         `saveOpacityFunc` once on the click-off close. Both are read UNGUARDED-if-set exactly as
         the reference writes them (an `if ( ... ) then` around each), so a caller that sets neither
         gets a slider that moves and does nothing — which is the reference's own behaviour, not a
         gap.

         `OpacityFrameCloseButton` is the reference's click-off catcher: declared inside this
         frame's `<Frames>` but RE-PARENTED to UIParent by its own `parent=` attr, `setAllPoints`
         over the whole screen, and dropped one frame level in OnLoad so it sits under the popup it
         closes. `RegisterForClicks("LeftButtonDown","RightButtonDown")` — it fires on the press,
         not the release, so a click anywhere outside dismisses immediately. -->
    <Frame name="OpacityFrame" toplevel="true" parent="UIParent" movable="true" enableMouse="true" hidden="true">
        <Size><AbsDimension x="80" y="180"/></Size>
        <Anchors><Anchor point="TOPRIGHT"><Offset><AbsDimension x="0" y="0"/></Offset></Anchor></Anchors>
        <Backdrop bgFile="Interface\DialogFrame\UI-DialogBox-Background" edgeFile="Interface\DialogFrame\UI-DialogBox-Border" tile="true">
            <BackgroundInsets><AbsInset left="11" right="12" top="12" bottom="11"/></BackgroundInsets>
            <TileSize><AbsValue val="32"/></TileSize>
            <EdgeSize><AbsValue val="32"/></EdgeSize>
        </Backdrop>
        <Frames>
            <Slider name="OpacityFrameSlider" orientation="VERTICAL" minValue="0" maxValue="1" valueStep="0.01" defaultValue="1">
                <Size><AbsDimension x="16" y="128"/></Size>
                <Anchors><Anchor point="TOP"><Offset><AbsDimension x="-10" y="-35"/></Offset></Anchor></Anchors>
                <Backdrop bgFile="Interface\Buttons\UI-SliderBar-Background" edgeFile="Interface\Buttons\UI-SliderBar-Border" tile="true">
                    <EdgeSize><AbsValue val="8"/></EdgeSize>
                    <TileSize><AbsValue val="8"/></TileSize>
                    <BackgroundInsets><AbsInset left="3" right="3" top="6" bottom="6"/></BackgroundInsets>
                </Backdrop>
                <Layers>
                    <Layer level="ARTWORK">
                        <!-- The title hangs off the FRAME, not the slider — the ref anchors it
                             `relativeTo="OpacityFrame"` from inside the slider's own layers. -->
                        <FontString name="$parentText" inherits="GameFontNormalSmall" text="OPACITY">
                            <Anchors><Anchor point="TOP" relativeTo="OpacityFrame"><Offset><AbsDimension x="0" y="-15"/></Offset></Anchor></Anchors>
                        </FontString>
                        <!-- The two end labels are UNNAMED in the ref, and the literals are the
                             ref's own: "0%"/"100%" are written as text, not as global strings. -->
                        <FontString inherits="GameFontHighlightSmall" text="0%">
                            <Anchors><Anchor point="BOTTOMLEFT" relativePoint="BOTTOMRIGHT"><Offset><AbsDimension x="2" y="3"/></Offset></Anchor></Anchors>
                        </FontString>
                        <FontString inherits="GameFontHighlightSmall" text="100%">
                            <Anchors><Anchor point="TOPLEFT" relativePoint="TOPRIGHT"><Offset><AbsDimension x="-2" y="3"/></Offset></Anchor></Anchors>
                        </FontString>
                    </Layer>
                </Layers>
                <Scripts>
                    <OnValueChanged>
                        if ( OpacityFrame.opacityFunc ) then
                            OpacityFrame.opacityFunc();
                        end
                    </OnValueChanged>
                </Scripts>
                <ThumbTexture file="Interface\Buttons\UI-SliderBar-Button-Vertical">
                    <Size><AbsDimension x="32" y="32"/></Size>
                </ThumbTexture>
            </Slider>
            <Button name="OpacityFrameCloseButton" parent="UIParent" setAllPoints="true" hidden="true">
                <Scripts>
                    <OnLoad>
                        this:SetFrameLevel(this:GetFrameLevel()-1);
                        this:RegisterForClicks("LeftButtonDown", "RightButtonDown");
                    </OnLoad>
                    <OnClick>
                        OpacityFrame:Hide();
                        if ( OpacityFrame.saveOpacityFunc ) then
                            OpacityFrame.saveOpacityFunc();
                        end
                    </OnClick>
                </Scripts>
            </Button>
        </Frames>
        <Scripts>
            <OnShow>
                OpacityFrameCloseButton:Show();
            </OnShow>
            <OnHide>
                OpacityFrameCloseButton:Hide();
            </OnHide>
        </Scripts>
    </Frame>
</Ui>
